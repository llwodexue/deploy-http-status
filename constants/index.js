// 是否开启 app cors，用来测试接口 cors
const CORS_ENABLE = false
// 是否开启 websocket
const WEBSOCKET_ENABLE = true
// 是否开启接口 token 校验，如果要测试接口 cors 改为 false
const VERIFY_ENABLE = false

module.exports = { CORS_ENABLE, WEBSOCKET_ENABLE, VERIFY_ENABLE }
