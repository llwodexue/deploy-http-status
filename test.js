let end = Date.now() + 1000 * 10
for (; Date.now() < end; ) {
  // console.log()
}

/* require('fs').readFile(require.main.filename, () => {})
let end = Date.now() + 1000 * 10
for (; Date.now() < end; ) {
  // console.log()
} */
